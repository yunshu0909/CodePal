# CC Switch 迁移调研 · 任务主控文档

## 0. 元信息
- 创建日期：2026-04-12
- 当前批次：Batch 3
- 自主度模式：b
- 下次汇报触发点：形成可执行迁移结论或出现需要用户拍板的架构分叉

## 1. 用户的任务（原话精炼）
- 调研 `CC Switch` 的实现逻辑，并评估如何尽可能完整地搬到当前 `skills / CodePal` 工具里。

## 2. AI 对任务的理解（确认时给用户看的版本）
- 核心：拆清 `CC Switch` 的产品能力、技术架构和关键实现，再映射到当前 `CodePal` 仓库，给出可落地迁移方案。
- 边界：本轮以调研、差异分析、迁移设计为主，不承诺一次性完成整套产品移植。
- 产出形态：研究报告 + 迁移蓝图 + 实施优先级建议。
- 用户已确认：✅ 2026-04-12（按原始任务默认假设继续推进：`CC Switch` 指 `farion1231/cc-switch`）

## 3. 任务队列

| # | 任务 | 状态 | 依赖 | 预期产出 | 工具建议 |
|---|------|------|------|----------|----------|
| T1 | 定位 `CC Switch` 官方源码与产品边界 | ✅ | - | 官方仓库、README、核心模块清单 | Git / Read |
| T2 | 梳理当前 `CodePal` 现有模块与底座能力 | ✅ | - | 现状模块图与关键文件落点 | Read / Grep |
| T3 | 分析 `CC Switch` 核心实现逻辑 | ✅ | T1 | 数据流、存储、适配、代理、同步逻辑 | Read |
| T4 | 对比两边的功能矩阵与架构差异 | ✅ | T2,T3 | 可复用 / 不可复用 / 缺失能力清单 | 推理 |
| T5 | 给出“完整搬迁”的实施策略 | ✅ | T4 | 分阶段迁移方案与风险判断 | 推理 |
| T6 | 产出仓库内研究文档 | ✅ | T5 | `FINAL-REPORT.md`、`PHASE-1-BLUEPRINT.md` | Edit |

## 4. 批次执行日志（倒序）

### Batch 3 - 2026-04-12
**本批跑了**：T4, T5, T6
**关键发现**：
- `CC Switch` 不是“供应商切换脚本”，而是以 SQLite 为中心的多应用配置中台。
- 当前 `CodePal` 已有 Skills / MCP / 用量 / 会话浏览 / 网络诊断骨架，但供应商域仍是 `Claude-first`。
- 想“完整搬进来”，第一步不是补页面，而是引入统一状态层与多应用适配器。
**队列校验结果**：
- 未新增任务；本轮目标已收敛为文档交付。
- 未删除任务。
- 维持文档交付优先级最高。
**触发条件**：触发了 §6.1，已形成重要交付物

### Batch 2 - 2026-04-12
**本批跑了**：T2, T3
**关键发现**：
- 当前仓库的 `electron/main.js` 为 724 行，已接近单文件 800 行红线。
- `registerProviderHandlers.js` 与 `providerSwitchService.js` 已支持自定义 provider registry，但运行时仍主要写入 Claude 配置。
- `CC Switch` 的 Session / MCP / Skill / Proxy 都由独立命令与服务层承载，不是直接绑在 UI 上。
**队列校验结果**：
- 新增 T5：单独总结“完整搬迁”的阶段化路线，避免只做静态对比。
- 其余任务保持不变。
**触发条件**：未触发

### Batch 1 - 2026-04-12
**本批跑了**：T1
**关键发现**：
- 官方仓库为 `https://github.com/farion1231/cc-switch`。
- 前端为 React + TypeScript，桌面壳与核心能力在 Tauri/Rust。
- 功能面覆盖 Provider / Proxy / MCP / Prompts / Skills / Sessions / Sync / Deep Link。
**队列校验结果**：
- 保留原计划，继续转向当前仓库现状梳理。
**触发条件**：未触发

## 5. 工作假设
- 🟢 H1：用户说的 `CC Switch` 指官方仓库 `farion1231/cc-switch`，而非仓库内旧版 provider switch 草稿。
- 🟢 H2：用户本轮最需要的是“如何搬”的可执行路线，而不是立即完成全量代码迁移。
- 🟢 H3：对当前 `CodePal` 来说，优先迁的是架构模式和关键能力，不是 Tauri/Rust 技术栈本身。

## 6. 触发用户介入条件
1. 完成里程碑批次（产出重要交付物）
2. Premise 被证伪
3. 真分叉决策
4. 连续 2 批无新进展
5. 意外重大发现
6. 需要用户真实数据

## 7. 决策记录
- D1（2026-04-12）：按默认假设将 `CC Switch` 识别为官方仓库 `farion1231/cc-switch`，先做完整调研再决定是否进入代码实现。
- D2（2026-04-12）：不建议做“逐文件硬搬运”，优先迁移其“统一状态中心 + 多应用适配器”架构。
- D3（2026-04-12）：研究交付落在仓库文档中，便于后续直接接 implementation。

## 8. 收尾产出
- [x] FINAL-REPORT.md
- [x] PHASE-1-BLUEPRINT.md
