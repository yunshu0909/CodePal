# [CodePal 会话状态] 配置
# 由 CodePal「会话状态」维护。目录名 k28-status-light 是旧状态灯留下的，为兼容已装的钩子不改名。

# 【总闸】CodePal 自动维护，请勿手改：1=钩子记录会话状态；0=钩子只做清理
STATUS_LIGHT_ENABLED=1

# ── 以下为旧状态灯的语音 / 亮灯 / 摘要设置，已停用 ─────────────────────
# CodePal 不再读取这些项，钩子脚本也不再使用；保留只为兼容旧安装，删掉也没影响。
VOICE_ENABLED=0
VOLC_API_KEY=
VOLC_SPEAKER=zh_female_roumeinvyou_emo_v2_mars_bigtts
VOLC_RESOURCE_ID=seed-tts-1.0
VOLC_SPEED=1.0
TTS_TIMEOUT_SECONDS=30
OUTPUT_DEVICE=
AUDIO_GUARD_ENABLED=0
LAST_SAFE_OUTPUT_DEVICE=
LAST_SAFE_INPUT_DEVICE=
TASK_SUMMARY_ENABLED=0
TASK_SUMMARY_MODEL=deepseek-v4-flash
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=
